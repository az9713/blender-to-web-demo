# Environment

- Date: 2026-09-15 local task date; build performed in this Codex desktop session.
- Working directory: local `three-band-energy-sculpture\energy-demo` project folder.
- OS host: Windows, local PowerShell execution.
- Blender executable: user-local Blender `5.2.1 LTS` installation; use `<path-to-blender.exe>` when reproducing.
- Blender version: `5.2.1 LTS`, hash `9e2066aef7ef`, build date `2026-08-25`.
- Python launcher: `Python 3.13.5`
- Node: `v22.22.0`
- npm: `9.9.4`
- Three.js delivery: vendored local files copied from the bundled reference project; runtime import map maps `three` to `public/vendor/three/three.module.js`.
- Local server: Node server bound to `127.0.0.1`, default port `4186`.

Image generation used the built-in Codex image generation tool. The final concept was copied into `references/concept.png`; the source prompt is in `references/image-generation-prompt.txt`.
